[user]
name = Rob Kennedy
# email set in .gitconfig.local

[core]
editor = vim
pager = less -RF
excludesfile = ~/dotfiles/gitexcludes

[color]
ui = auto

[alias]
info = show --stat
tree = log --graph --pretty=pretty --parents HEAD
root = rev-parse --show-toplevel
co = checkout

[http]
# See http://stackoverflow.com/a/8467406/33732 for sslCAinfo

[pretty]
pretty = %C(yellow)%h%Creset -%Cred%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset

[include]
path = ~/.gitconfig.local

[push]
default = simple

[pager]
status = true

[help]
autocorrect = 10
